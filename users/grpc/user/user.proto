syntax = "proto3";

package user;

option go_package = "/;user";

message User {
  string user_name = 2;
  string password = 3;
}
message ListUserRequest {
}

message ListUserResponse {
  repeated User profile=1;
}

service UserService {
  rpc ListUser(ListUserRequest) returns (ListUserResponse);
}